<x-layout>

    <div class="grid md:grid-cols-4 sm:grid-cols-3 gap-4">
        <x-card class="flex justify-between">
            <h3>{!! __('app.total_visits') !!}</h3>
            <h3>{{ $total_visits }}</h3>
        </x-card>

        <x-card class="flex justify-between">
            <h3>{!! __('app.total_visits') !!}</h3>
            <h3>{{ $total_visits }}</h3>
        </x-card>

        <x-card class="flex justify-between">
            <h3>{!! __('app.total_visits') !!}</h3>
            <h3>{{ $total_visits }}</h3>
        </x-card>
    </div>

</x-layout>
